name: Cache Workflow
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        id: cache
        with:
          path: ~/.cache
          key: cache-b109f58
      - name: prime-cache-b109f58
        run: echo "Cache hit = ${{ steps.cache.outputs.cache-hit }}"
